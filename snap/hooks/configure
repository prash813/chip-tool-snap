#!/bin/sh
VAR="$(snapctl get  paa-path)"
if [ -z "$VAR" ]
then
	unset CHIPTOOL_PAA_TRUST_STORE_PATH
else
	export CHIPTOOL_PAA_TRUST_STORE_PATH=$SNAP_COMMON/$VAR/ 
fi

echo $CHIPTOOL_PAA_TRUST_STORE_PATH &> $SNAP_COMMON/configure.log 

